# Foo Legacy Dashboard

A small internal-tools dashboard project from before the v2 reposition. The project covers a stats overview, a settings page, and a launch checklist. We tracked these in the legacy `.overdrive/` layout for several sessions before the migration.

Users: internal ops team (~12 people).
Stack: Node + Postgres + a React frontend.
