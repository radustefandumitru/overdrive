---
ovd-plan: true
version: 3
project: "Descriptions and code blocks"
---

# Descriptions and code blocks

## I. Node with prose and an untagged yaml block []

This is a regular prose description.

It can span multiple paragraphs and contain untagged code blocks like this one:

```yaml
this: is
not: an
ovd: plan
block: true
```

And another in a different language:

```js
console.log("description code")
```

The yaml ovd-plan annotation below is the actual annotation:

```yaml ovd-plan
skills: [planning-first]
confidence: medium
```
