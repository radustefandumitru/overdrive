Run:

```bash
overdrive doctor --project-dir "$PWD"
```

Explain any ovd-workflow issues without changing project code. If the command is unavailable, explain that the full Overdrive CLI needs to be installed first.
