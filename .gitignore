.DS_Store
*.zip
overdrive-*.tgz
package-lock.json
sources.lock.json

# Do not commit raw/private skill snapshots. Public installs pull from upstream
# through manifest.json and pinned official installers.
bundled/skills/
.overdrive/
