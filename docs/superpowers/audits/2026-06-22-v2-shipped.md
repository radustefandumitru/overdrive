# v2 Audit — 2026-06-22

> Independent verification pass per `17-audit-readiness.md` (internal working notes, not shipped in the public repo). The audit answers one question: **does the codebase as shipped meet every contract it was built against** (r3 spec + impl-plan §5 done-definitions + locked Q-decisions + Phase 2 patterns)? NOT a code review; NOT a re-design.

> Discipline: **contracts, not vibes.** Every verdict is evidence-backed (file:line / test name / log entry / commit hash) or it doesn't count. No shipped code was modified during the audit.

> Reproducibility (QA.5): a second agent can replay every verdict below from the cited evidence. Structural checks use `rg`/`git`/`node`; behavioral checks use the existing test suites + the smoke reproduction.

---

## Executive summary

**Overall verdict: ✅ PASS — v2 is ready for GA.**

| Verdict | Count |
|---|---|
| PASS | 6 audit tasks + 10 cross-phase invariants + 9 r3 sections + 7 phase done-definitions + 8 failure-mode hunts |
| PARTIAL | 0 |
| FAIL | 0 |
| FOLLOW-UP | 6 (all documented in impl-plan §6 / r3 §13.3; none GA-blocking) |

Against the QA.3 GA threshold (zero FAIL; PARTIAL ≤ 5 with acceptance; FOLLOW-UP unlimited): **met** — zero FAIL, zero PARTIAL.

**Top findings requiring user attention:**
1. **FU-1 (actionable):** `/ovd-plan edit` does not auto-trigger DOC UPDATE — it emits a text recommendation only (`edit.js:605`), even though `runDocUpdate` now exists and is wired into `/ovd-log` default + handoff. Documented as Q3.6.1; the in-code "deferred to Phase 7" comment lapsed to post-v1. Low-cost to wire in v1.0.x.
2. **No scope creep:** the two "must-not-land" deferrals (Q8 auto-route IDEA→EDIT; Q9 configurable blind-spot categories) genuinely did NOT land — confirmed in code. No documented deferral accidentally shipped.
3. **Test suite reconciled exactly:** measured 5009 ovd-plan checks == Phase 7 wrap-up log; no coverage regression.
4. **Smoke exercises all non-happy paths:** iteration / FIX-escalation / `--small` growth / DECISION POINT — confidence-laundering hunt clean.
5. **Five spec-vs-code wording gaps** (Q3.3A.10, Q3.3C, Q3.4.2, Q4.2.1, Q4.3.1) are documented FOLLOW-UPs — r3 example/citation text trails the (correct) shipped code. Cosmetic; resolve via an r4 doc amendment when convenient.

---

## Audit-design decisions (QA.1–QA.10 — LOCKED 2026-06-22)

User accepted all 10 brief-17 recommendations as-is.

| # | Decision |
|---|---|
| QA.1 | AUDIT.md at `docs/superpowers/audits/2026-06-22-v2-shipped.md` (new dir, committed when complete). |
| QA.2 | Report-only for v1. No auto-fix; remediation is a separate effort. |
| QA.3 | GA threshold: zero FAIL; PARTIAL ≤ 5 with explicit per-finding acceptance; FOLLOW-UP unlimited. |
| QA.4 | Verify contracts, not the user mental model. Mental-model discipline applies only to how findings are surfaced. |
| QA.5 | Reproducible by a second agent. |
| QA.6 | Strict scope: contracts only. Style/design observations out of scope. |
| QA.7 | Failure-mode hunts time-boxed ~30 min each; document depth + incomplete coverage. |
| QA.8 | §6 / §13.3 deferrals = FOLLOW-UP by default; a deferral that accidentally landed = FAIL. |
| QA.9 | Findings inline in AUDIT.md; ticketing is the user's choice. |
| QA.10 | One-time at v2 GA; re-run cadence is post-v1. |

**Verdict rubric:** PASS = evidence located, behavior matches, tests cover. PARTIAL = matches but tests undercover OR partial behavior. FAIL = evidence missing OR contradicts contract. FOLLOW-UP = explicit post-v1 deferral per §6 / §13.3.

---

## Test suite green confirmation (Task A.5) — ✅ PASS

Environment: branch `feature/ovd-plan`, top commit `4b3dc25` (Phase 7 wrap-up, v2-done marker). Working tree clean except two pre-existing untracked spec docs. All commands run 2026-06-22 by the auditor.

| Command | Exit | Result | Verdict |
|---|---|---|---|
| `npm run check` | 0 | All `node --check` syntax checks pass (~95-file parse chain) | PASS |
| `npm test` (fast) | 0 | check → consistency → test:workflow → test:ovd-plan → test:smoke end-to-end | PASS |
| `npm run test:ovd-plan` | 0 | **5009 checks passed** | PASS |
| `npm run test:workflow` | 0 | "ovd-workflow tests passed" | PASS |
| `npm run eval:router` | 0 | "Router benchmark validation passed (269 checks)." | PASS |

Supplementary: smoke 35; consistency 1181; router 269; 44 ovd-plan test suites.

**Count reconciliation:** measured `test:ovd-plan` = **5009** = Phase 7 wrap-up §7 log verbatim (impl-plan line 3855: "5009 checks (4939 Phase-6 close + 70 verify-layout)"). Brief 17's "~2000–2500" estimate (line 186) predates the Phase 3–7 suite expansion and is stale; the kickoff designates the log as source-of-truth. **No coverage regression.**

---

## Smoke test independent reproduction (Task A.6) — ✅ PASS

**Target:** `scripts/smoke-test-ovd-plan.js` (Phase 7 Task 7.1).

### Non-happy-path coverage (source review — guards Failure Mode #7)

The test drives the real handlers on the committed fixture; every agent return is a canned `--entries-json` payload (Pattern 1 — no LLM). Required non-happy paths present and asserting real conditions:

| Path | Location | Assertion |
|---|---|---|
| Iteration loop | §6 `iterate II.1.a` | records `iterations[]` |
| FIX escalation at 2-attempt cap | §7 `fix II.1.b` ×3 | `escalated === true` / `mode === 'fix-escalate'` |
| `--small` scope-growth | §8 `monitor` 5 files | `r.exceeded === true` |
| DECISION POINT (Q7.1) | §9 surface + resolve | persisted to decisions.md |

Plus recursive close cascade (§5), resume simulation (§11), handoff (§12), milestone-close (§13), final `verifyPlanLayout` with `counts.error === 0` (§14). Not golden-path-only.

### Execution + artifact inspection

`node scripts/smoke-test-ovd-plan.js --verbose` → **35/35 PASS, exit 0**. The shipped test tears down its temp dir, so the auditor ran an audit-side reproduction (`/tmp/ovd-audit-smoke.js`, outside the repo) preserving artifacts at `/tmp/ovd-audit-artifacts/app`:

| Artifact | Finding | Verdict |
|---|---|---|
| `OVERDRIVE.md` (final) | Parses clean. Milestone I + I.1/I.2/I.3 `[done]`, moved **verbatim** into `<!-- ovd-plan:archive -->` with close header. II.1.a carries `iterations[]`; II.1.b carries `fix_attempts[]` len 2 (cap). Schema v3, hierarchical IDs, deliberation-state markers preserved. | PASS |
| `.overdrive/decisions.md` | DECISION POINT row persisted with rationale (Q7.1 durability). | PASS |
| `handoffs/<ts>.md` | Well-formed (summary → highlights → follow-ups). | PASS |
| `reports/milestone-I-summary.md` | Computed signals (3/3 done) consistent with archive. | PASS |
| `sessions/*` + event files | Structured (`status_after`, `files_touched`); ISO-stamped per-event files. | PASS |
| `plan.cache.json` | Valid JSON (verified `JSON.parse`); mirrors frontmatter + tree. | PASS |

### Two observations — RESOLVED as non-findings against r3

- **OBS-1** — frontmatter `active_node`/`current_milestone` still point at closed milestone I after II became active. r3 §7.5 step 4 / §8.2 surface "Active focus is now II.3" as **agent next-step text**; §7.1 step 2 has `/ovd-log` update `active_node` *from the agent's state payload*. The smoke's `close`/`milestone-close` calls passed no active-node update, and **no r3 clause mandates the CLI auto-advance `active_node` on close**. → within contract; test-fixture artifact. Cache mirrors source faithfully (not a cache bug).
- **OBS-2** — `session_count: 0` after save+handoff. `session_count` is frontmatter metadata (§9.3 shows `4` as a value); **no r3 clause mandates a per-log CLI increment**. → no contract backing; not a finding.

**A.6 PASS.**

---

## Per-phase done-definition verification (Task A.1)

Evidence: impl-plan §5 done-defs + readiness briefs + shipped modules + the §7 per-suite test counts.

### Phase 1 — Foundation — ✅ PASS
| Done-def bullet | Evidence | Verdict |
|---|---|---|
| Parser/writer round-trip canonical example zero-diff | `parser.js` (104 checks), `writer.js` (28); A.6 final OVERDRIVE.md parses + round-trips | PASS |
| Cache mirrors + walks tree for closure | `cache.js` (39); `closureCheck` def `cache.js:131` | PASS |
| Skill-router accepts prior_set; existing tests pass | `skill-router.js` (53); router benchmark 269 green | PASS |
| CLI skeletons callable | `index.js` dispatch; 4 commands wired | PASS |
| `.overdrive/` scaffolding + gitignore carve-out | `fs.js` (59); A.6 scaffold produced full §9 layout | PASS |

### Phase 2 — /ovd-workflow — ✅ PASS
| Bullet | Evidence | Verdict |
|---|---|---|
| Bare `workflow` → tutorial + status + action-path | `workflow.js` (204) | PASS |
| `init` orchestrates with per-step approval + migration detection | `workflow.js`, `migrate.js` (150) | PASS |
| `map` runs 5 mappers → 5 files | `codebase-mapper.js` (135) | PASS |
| Drift flags only affected mappers | `drift-detector.js` (99) | PASS |
| Refresh updates only flagged mappers | `codebase-refresh.js` (124) | PASS |
| Preferences + requirements via Socratic flows | `preferences-elicit.js` (80), `requirements-draft.js` (88) | PASS |
| Legacy layout migrated/archived | `migrate.js`; Pattern 5 seam tests | PASS |
| Task 2.9 deferred to Phase 7 (per done-def) | landed Phase 7, commit `f57abb6`; deprecation notes verified | PASS |

### Phase 3 — /ovd-plan — ✅ PASS (1 sub-item → FU-1)
| Bullet | Evidence | Verdict |
|---|---|---|
| Bare `/ovd-plan` routes DISPLAY/NEW/CONTINUE | `display.js` (137), `index.js` routing | PASS |
| New-project flow → complete leaf contracts | `deliberate.js` (324), `plan-skills.js` (196), `plan-quality.js` (208) | PASS |
| Idea flow action-path; never silently edits | `idea.js` (169); approval-gate + new-chat, no `runEdit` | PASS |
| Edit, research, plan-quality pass tests | `edit.js` (221), `research.js` (186) | PASS |
| EDIT triggers internal DOC UPDATE | `edit.js:605` emits recommendation only; `runDocUpdate` exists but unwired here | **FOLLOW-UP (FU-1)** |
| Deliberation state persists; re-entry honors user | `deliberation-state.js` (93), `reentry.js` (227) | PASS |
| Blind-spot Stage 3, terse external | `blind-spot.js` (242); fixed 11 categories | PASS |

### Phase 4 — /ovd-go — ✅ PASS
| Bullet | Evidence | Verdict |
|---|---|---|
| Bare `/ovd-go` orientation + action-path; never silent | `orient.js` | PASS |
| Leaf execution invokes skill prior | `execute.js`; reads annotation (no router call) | PASS |
| Awaiting-review always prompts; never auto-promotes | `review.js`; only explicit approval → done | PASS |
| Iteration loop captures + re-executes deltas | `iterate.js`; A.6 `iterations[]` written | PASS |
| Recursive close walks up with prompts | `closure.js` → shared `closureCheck` | PASS |
| `--small` auto-detect + scope-growth | `small.js`; A.6 `exceeded===true` | PASS |
| Two-attempt cap + escalation | `fix.js`; A.6 `escalated` at cap | PASS |
| Node-ref fuzzy + ambiguous + tie-break announce | `noderef.js` | PASS |

### Phase 5 — /ovd-log — ✅ PASS
| Bullet | Evidence | Verdict |
|---|---|---|
| DEFAULT → session file + recursive close check | `log-default.js`; A.6 session file written | PASS |
| HANDOFF full 11-step pipeline | `handoff.js`; A.6 handoff file written | PASS |
| Capture, concerns, doc-update per spec | `log-capture.js` (60), `concerns.js`, `doc-update.js` | PASS |
| Milestone close + learnings + release + archive | `milestone-close.js`; A.6 milestone summary + verbatim archive | PASS |
| Commit gated by user approval | `commit.js`; runs only on `confirm`; no `--no-verify` | PASS |
| Recursive close shared (no fork) | `closure.js` imports `cache.closureCheck` | PASS |

### Phase 6 — Intent Detection — ✅ PASS
| Bullet | Evidence | Verdict |
|---|---|---|
| Free-form routes correctly for matrix | `intent.js` (102; 29-case matrix incl. near-miss) | PASS |
| Ambiguous → action-path prompts | `intent.js` renderer; 2–4 options + escape | PASS |
| Mis-classifications captured | `intent.js` logging → session capture | PASS |
| Categorical confidence (no numeric) | `CONFIDENCE_VALUES = {unambiguous, ambiguous}` | PASS |
| Explicit `/ovd-*` bypass | `decision:'bypass'` (r3 §3.4) | PASS |

### Phase 7 — Polish + integration — ✅ PASS
| Bullet | Evidence | Verdict |
|---|---|---|
| Smoke passes end-to-end | 35/35 (A.6) | PASS |
| Global instruction note installed | Task 7.2, commit `6d35250`; `runVerify` content check | PASS |
| Verify command extended | `verify-layout.js` (70); `overdrive verify --plan` opt-in | PASS |
| Test suite green | 5009 + smoke 35 + router 269 | PASS |
| README updated | Task 7.5, commit `7aa286f` | PASS |
| Install no longer pollutes project dirs | Task 7.6, commit `0bda4e0`; `installLocal:false` default | PASS |

---

## Cross-phase invariant check (Task A.2) — ✅ ALL PASS

| Invariant (briefs 12–16) | Evidence | Verdict |
|---|---|---|
| CLI never makes LLM calls | `rg` fetch/api/anthropic/openai over `lib/ovd-plan/` = ZERO; 4 `installer.js` hits are filenames/user-config docs, not calls | PASS |
| CLI never writes content agent should own | Handlers receive structured `--entries-json`; Pattern 1 dispatch | PASS |
| Recursive close uses shared helper (no fork) | `closureCheck` single def `cache.js:131`; `closure.js:9` "don't fork" comment; both /ovd-go + /ovd-log paths | PASS |
| No silent skill re-resolution at execution | `execute.js` reads annotation; re-resolve only when "empty or low-confidence" (line 149), agent-side | PASS |
| Mode switches transparent (small↔full) | `small.js` action-path prompts | PASS |
| Status transitions via writer.js only | single `writeOverdriveMd` + `statusBracket` | PASS |
| Archive preserves verbatim (no summarization) | A.6 final OVERDRIVE.md: milestone I archived verbatim | PASS |
| Commits always user-approved; hooks not skipped | `commit.js`: git only on `confirm`; "NEVER --no-verify/--no-gpg-sign" | PASS |
| Categorical confidence only | `intent.js` Set{unambiguous,ambiguous} | PASS |
| Explicit slash commands bypass classification | `intent.js` `decision:'bypass'` | PASS |
| Migration-compat preserved (Pattern 5) | ~30 test files cover migration-compat/legacy seams | PASS |
| Default install footprint user-global only | `installer.js` `installLocal:false`; `--install-local` opt-in | PASS |

---

## Pattern enforcement check at scale (Task A.3) — ✅ ALL PASS

Handler scorecard (representative dispatch handlers × 8 Phase-2 patterns). Cell = PASS unless noted.

| Handler group | P1 no-LLM | P2 canonical prims | P3 flags | P4 JSON guard | P5 migration seam | P6 log-push | P7 action-path | P8 coverage |
|---|---|---|---|---|---|---|---|---|
| workflow.js | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ (525) | ✓ | 204 |
| deliberate/plan-skills/plan-quality | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 324/196/208 |
| idea/edit/research | ✓ | ✓ (`appendUnderHeader`/`appendDecision`) | ✓ | ✓ | ✓ | ✓ | ✓ | 169/221/186 |
| orient/execute/review/iterate | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | covered |
| fix/decision/small/noderef/closure | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | covered |
| log-default/handoff/capture/concerns/milestone-close/doc-update | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 60+ each |
| intent.js | ✓ (agent-side) | ✓ | ✓ | ✓ | ✓ | ✓ (454) | ✓ | 102 |
| verify-layout.js | ✓ | ✓ (reuses parser/cache) | ✓ (`--plan`) | n/a | ✓ | n/a | n/a | 70 |

**Pattern evidence highlights:**
- **P1/P4** — every `entriesJson` handler in `index.js` does `try JSON.parse → on fail return {ok:false,reason}` with NO write before the guard.
- **P2** — single canonical primitives: `appendUnderHeader` (1 def `migrate.js:167`, 9 consumers), `appendDecision` (1 def `decisions-log.js`, 6 consumers), `parseFrontmatter` (1 def `parser.js`), `closureCheck` (1 def `cache.js`). No re-implementations.
- **P3** — flag set limited to `--entries-json`, `--project-dir`, `--small`, `--plan`, `--install-local`, `--color`, `step`/`confirm`. No scope-creep flags.
- **P6** — `note: sub.summary || sub.note` graceful degradation (`workflow.js:525`, documented `intent.js:454`).
- **P7** — numbered options + "describe other" escape across slash bodies + CLI prompts (idea, orient, review, closure, small, decision).
- **P8** — per-handler counts mostly 80–324. Primitives below 80 (`writer.js` 28, `cache.js` 39, `skill-router.js` 53) are infra, not handlers — the ≥80 heuristic is handler-targeted; acceptable. No handler exceeds the 200-as-implementation-detail concern in a way that flags.

---

## r3 spec coverage check (Task A.4) — ✅ PASS (FOLLOW-UP items noted)

| r3 § | Owning phase | Evidence | Verdict |
|---|---|---|---|
| §3 Intent Detection | Phase 6 | `intent.js`: classify + ambiguity template + bypass + mis-class logging | PASS |
| §4 /ovd-workflow | Phase 2 | `workflow.js` + 5 mappers + drift/refresh + preferences/requirements + decisions | PASS |
| §5 /ovd-plan | Phase 3 | DISPLAY/DELIBERATE/IDEA/EDIT/RESEARCH + Stages 1–8 + RESOLVE SKILLS + blind-spot + re-entry. §5.2 IDEA always-approval (Q8 not auto-routed). EDIT→DOC UPDATE = **FU-1**. RESEARCH node-attach = **FU-2** | PASS (2 FU) |
| §6 /ovd-go | Phase 4 | ORIENT/EXECUTE/VERIFY/AWAITING-REVIEW/ITERATION/recursive-close/--small/FIX/DECISION/node-ref | PASS |
| §7 /ovd-log | Phase 5 | DEFAULT/CAPTURE/CONCERNS/HANDOFF (11-step) + recursive close + milestone close | PASS |
| §8 Cross-pipeline flows | Phases 4/5/7 | pause-resume + close-up-chain verified via smoke (A.6) | PASS |
| §9 File & folder structure | Phases 2/7 | `fs.js` scaffold; A.6 artifact tree matches §9 layout; git carve-out | PASS |
| §10 Node schema | Phases 1/3/5 | `parser.js`/`writer.js`; hierarchical IDs; `active_node` canonical (no `active_focus` drift). §10.1 verbose names vs writer-canonical = **FU-3** (Q3.3A.10) | PASS (1 FU) |
| §11 Skill-router protocol | Phases 1/3/4 | `skill-router.js` helper; SKILL.md additive section; RESOLVE SKILLS 4-field; execution reads annotation; no code-level `route()` (per §11.1) | PASS |

---

## Failure mode hunts (time-boxed ~30 min each — QA.7)

| # | Hunt | Method | Result |
|---|---|---|---|
| 1 | Silent r3 drift | `rg active_focus` (none); field-name set in parser/writer | **Clean.** `active_node` canonical; writer uses `success`/`deps`/`verify`/`scope.in`. The §10.1-vs-writer divergence is the *documented* FU-3 (Q3.3A.10), not silent. |
| 2 | Pattern violations as refactors | `git log -i --grep dedup\|shared\|simplif\|cleanup\|refactor` | **Clean.** "shared" commits are the intended `closureCheck`/`runRecursiveCloseCheck` no-fork utilities; "Simplify public archive" is a pre-ovd-plan commit. |
| 3 | Test coverage gaps on new edges | per-suite counts in §7 log | **Clean.** New edges have dedicated suites: blind-spot 242, reentry 227, edit 221, plan-skills 196, plan-quality 208, intent 102 (29-case matrix). |
| 4 | Boundary breaks | Pattern 1/4 (A.3) | **Clean.** Zero LLM calls in CLI; JSON-guard-before-write uniform. |
| 5 | Over-flagging via loose matching | noderef tie-break/announce; intent near-miss tests | **Clean.** intent 29-case matrix includes near-miss + bypass; noderef announces auto-pick with cancel. |
| 6 | Pragmatic-violates-invariant | `git log -i --grep pragmatic\|simpler\|easier` | **Clean.** No matches. |
| 7 | Confidence laundering | read smoke source | **Clean.** All 4 non-happy paths exercised with real assertions (A.6). |
| 8 | Unscoped deferrals | `rg TODO\|FIXME\|defer.*phase`; cross-ref §6; confirm Q8/Q9 not landed | **One scoped deferral surfaced (FU-1, edit.js:605) — it IS scoped (Q3.6.1), not unscoped.** Q8 + Q9 confirmed NOT landed (no scope creep). |

**Coverage note:** all 8 hunts completed within budget. Depth was sufficient to reach a verdict; no hunt was left incomplete.

---

## Findings requiring remediation (PARTIAL + FAIL)

**None.** Zero FAIL, zero PARTIAL.

---

## Known-issues acceptance (FOLLOW-UP — all documented in impl-plan §6 / r3 §13.3)

| ID | Title | Location | Reason / disposition |
|---|---|---|---|
| FU-1 | EDIT does not auto-trigger DOC UPDATE | `edit.js:605` | Q3.6.1. `runDocUpdate` exists + wired into /ovd-log default+handoff; EDIT emits recommendation only. **Actionable**: mechanical wiring (`runDocUpdate(rootDir,{changedNodes})` after EDIT apply); recommend in v1.0.x. In-code "Phase 7" comment lapsed to post-v1. |
| FU-2 | RESEARCH "attach to specific node" not implemented | `research.js` | Q3.7.1. Ships inbox + sessions output; node-attach needs `references.research[]` + `attach_to_leaf` flag. Deferred per orchestrator. |
| FU-3 | r3 §10.1 leaf field names vs writer-canonical | r3 §10.1 (doc) | Q3.3A.10. Spec example shows `success_criteria`/`dependencies`/`scope.files_touched`; code uses round-trip-canonical `success`/`deps`/`scope.in`. r4 doc amendment, not a code change. |
| FU-4 | `/ovd-plan verify` undocumented in r3 | r3 (no §) | Q3.3C. Code surface shipped (Slice C); r3 lacks the section. r4 amendment to document retrospective-audit semantics. |
| FU-5 | Blind-spot tag form example divergence | r3 §5.3.4 vs §10.4 | Q3.4.2. Code ships the authoritative verbose `[proposed-by-agent: …]`; §5.3.4 example still shows short `[agent]`. r4 example update. |
| FU-6 | Stale spec wording: §5 Task 4.2 `route()` + Task 4.3 "§6.8 table" citation | impl-plan §5 (doc) | Q4.2.1 + Q4.3.1. Code is correct (Pattern-1 dispatch reading annotation; `KNOWN_METHODS` set); the §5 task wording trails it. r4/impl-plan amendment. |

All six are doc-trailing-code or explicitly post-v1; none affect shipped behavior or block GA.

**Post-audit remediation status (2026-06-22, same session):**
- **FU-3/4/5/6 (doc-sync) — resolved in place:** r3 §10.1 + §5.6 field names → writer-canonical; r3 §5.8 added for `/ovd-plan verify`; r3 §5.3.4 example tag; impl-plan §5 Task 4.2/4.3 wording + citation. Consistency (1181) re-run green.
- **FU-1 (EDIT→DOC UPDATE) — design LOCKED + partially shipped (TDD):** `edit.js` now leads with a doc-propagation action-path via `/ovd-log` and no longer recommends a codebase-mapper refresh (bug fix). The deeper auto-doc-write chain remains a v1.0.x item. Full suite green (`test:ovd-plan` 5010, +1). Design in impl-plan §6.1.
- **FU-2 (RESEARCH node-attach) — SHIPPED (TDD, 2026-06-23):** `attach_to_leaf` binds a substantive findings file to a leaf's `references.research[]` (pointer, not paste) via the committed-tree round-trip; leaf validated before write (no orphan files). r3 §10.8 documents the schema. `test:ovd-plan` 5027 / smoke 35 / consistency 1181 — all green.
- Also captured: v2.5 internal-state-chaining audit + v3 preference-toggle (impl-plan §6.1).

---

## Next steps recommendation

**Overall verdict PASS → v2 is ready for GA.**

1. **Commit this AUDIT.md** (the only Phase-A commit; no code changes).
2. **Merge `feature/ovd-plan` → `main`** when ready. The handoff dossier (00–18) + AUDIT.md is the durable v2 artifact.
3. **Optional v1.0.x maintenance** (user's choice, none GA-blocking):
   - FU-1: wire `edit.js` apply → `runDocUpdate` (low-cost, highest-value of the six).
   - FU-3/FU-4/FU-5/FU-6: bundle into a single r4 / impl-plan doc-amendment pass to close the spec-vs-code text gaps.
   - FU-2: implement `references.research[]` node-attach only if the surface is wanted.

---

## Auditor progress log
- **2026-06-22 (single session):** Step 0 env confirmed. A.5 PASS (5009 reconciled). QA.1–QA.10 locked. A.6 PASS (35/35 + artifact inspection; OBS-1/OBS-2 resolved as non-findings vs r3). Read contracts: r3 §3–§11, impl-plan §5 (all 7 done-defs) + §6 + Phase 7 wrap-up; dossier maps 08/00/17; briefs 12–16 invariants. Ran A.1/A.2/A.3/A.4 + 8 hunts via structural `rg`/`git`/`node` checks + the test suites + smoke reproduction. Verdict: **PASS**, 0 FAIL / 0 PARTIAL / 6 FOLLOW-UP. AUDIT.md complete — ready to propose commit.
